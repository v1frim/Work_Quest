# DESIGN.md — дизайн-система Work_Quest

Це **закон для будь-якої зміни інтерфейсу** в цьому репозиторії. Читається разом
із `CLAUDE.md`. Якщо нова фіча вимагає порушити правило звідси — спершу міняється
цей файл (свідомо, окремим комітом), а не тихо порушується правило.

Мета одна: **стриманий сучасний застосунок, а не ялинка.** Око має одразу бачити
головне (XP сьогодні, поле вводу), а не десять речей однакової гучності.

---

## 1. Колір

### 1.1 Один акцент

Акцент — бурштин `#e3aa4e` (токен `--gold`). Він означає рівно одне: **XP і
прогрес**. Дозволені місця:

- цифри XP і рівня;
- заливки прогрес-барів (XP, термометр планів, бар цілі);
- одна головна CTA на екран («Рекорди»);
- фокус/активний стан (`--sh-glow` — це фокус-кільце, не сяйво).

**Заборонено:** акцент у заголовках панелей, рамках карток, ховерах другорядних
кнопок. Якщо акцентом підсвічене все — він не означає нічого.

### 1.2 Нейтральна основа

Полотно `--bg #0c0d10`, поверхні `#15171c…#1d2026` — **соліди**. Ієрархія тексту —
шкала прозорості білого (`--tx…--tx-6`), нових сірих не заводити.

### 1.3 Кольори даних — це дані, а не декор

Кольори складностей і ліг живуть у реєстрах (`localStorage`) і редагуються
користувачем. В інтерфейсі вони показуються **малими дозами**: крапка 8px,
лівий кант 3px, дрібний підпис. **Ніколи** — як заливка цілої кнопки, фон рядка
чи сяйво. Десять складностей × повна заливка = веселка, з якої око не зчитує
нічого.

### 1.4 Статуси

`--st-done` зелений, `--st-fail` червоний, `--st-active` блакитний,
`--st-new` теплий. Тільки за прямим значенням (успіх/помилка/активність),
не «для краси».

## 2. Фон

Фон — майже чорний солід. Фази доби (`data-tod`) лишаються як механіка, але
дають лише **ледь помітний тінт** (радіальна пляма ≤6% прозорості вгорі екрана).
Зоряного неба, туманностей і блакитного дня немає: картки завжди стоять на
спокійному нейтральному полотні. Якщо тінт видно на скріншоті одразу — він
занадто сильний.

## 3. Поверхні, межі, тіні, радіуси

- **Один спосіб відділити поверхню:** рамка `rgba(255,255,255,0.07)` +
  (для плаваючих острівців) одна м'яка тінь `--sh-card`. Не «рамка + тінь +
  внутрішнє світіння» разом.
- Градієнтних поверхонь немає. Градієнтного тексту немає.
- Сяйв (`box-shadow: 0 0 Npx колір`) немає ніде. `--sh-glow` — фокус-кільце.
- Радіуси: картка 16 / бокс 12 / кнопка 10 / дрібне 7 / pill. Нових не заводити.

## 4. Типографіка

- Шрифт: **Inter** (Google Fonts) із системним фолбеком; `tabular-nums` — цифри
  в колонках не «танцюють».
- Шкала розмірів: **11 / 12 / 13 / 15 / 20**. Нові розміри не заводити.
- Мікрозаголовки секцій: 10–11px, uppercase, letter-spacing 1.2–1.6px, `--tx-5`.
- Ваги: 400 текст · 600 підписи · 700 значення · 800 великі цифри.
- Заголовок застосунку — стриманий ворднарк (20px, розрядка), без золота
  на пів екрана.

## 5. Іконки

- **Емодзі в хромі інтерфейсу заборонені** (заголовки панелей, кнопки,
  шапки оверлеїв, рядки статистики). Хром — це текст.
- Емодзі допустимі лише там, де вони — **дані користувача** (значок складності
  в реєстрі, значок ліги) або **семантична легенда** (медалі ✅/🥇 у прогресі днів).
- Складність у списках позначається **крапкою її кольору**, не емодзі.

## 6. Відступи

Сітка **8px**: зовнішні відступи 16/24, внутрішні 8/12/16, щілини 6/8/12.
Довільних значень на кшталт 13px не заводити (успадковані — виводити поступово).

## 7. Рух

Тільки те, що відповідає на дію користувача: ховер (колір/фон, 150мс),
натискання (1px вниз), поява тоста, заповнення барів (600мс). Пульсацій,
паралаксів і анімованих фонів немає. `prefers-reduced-motion` вимикає все.

## 8. Чекліст перед комітом будь-якої UI-зміни

1. Акцент з'явився в новому місці? → звір із 1.1, найімовірніше має бути нейтраль.
2. Колір складності/ліги десь став фоном або сяйвом? → крапка/кант (1.3).
3. Додався емодзі в хром? → прибрати (5).
4. Новий розмір шрифту, радіус чи сірий? → використай наявний токен.
5. `?tod=day` і `?tod=night` виглядають однаково спокійно? (2)
6. `./verify.sh` голим рядком + скріншот у Chromium до/після.
