# handoff.md — контекст проєкту Work_Quest

Довідник стану проєкту для нових сесій. Читається на старті **завжди** (див. `CLAUDE.md`).
Дві частини: **ДОВІДНИК** (актуальний стан) і **ЖУРНАЛ СЕСІЙ** (зворотно-хронологічний).

---
---

# ЧАСТИНА I · ДОВІДНИК

## 1. Що це за продукт

**Work_Quest** — персональний гейміфікований трекер робочих задач.
Продакшен: https://v1frim.github.io/Work_Quest/

Користувач виконує робочі задачі. Кожна виконана задача дає XP залежно від
складності. XP копиться → росте рівень. За віхи — досягнення. Ведеться щоденна
й загальна статистика, б'ються власні рекорди.

**Головна теза, з якої випливає кожне рішення в інтерфейсі:**

> Це **не менеджер задач**, а **мотиваційна панель**. Користувач відкриває її
> вранці, бачить свій прогрес, і йому хочеться закрити ще одну задачу.

Практичний наслідок: жодних дедлайнів, прострочень, червоних міток і списків
боргів. Перший екран завжди про **зроблене**, не про невиконане.

**Сестринський проєкт:** вокабуляр-тренажер Oxford_1000
(https://v1frim.github.io/Oxford_1000/, репозиторій `v1frim/Oxford_1000`).
Work_Quest переймає його стек, дизайн-систему, розкладку і рівень продуманості.
Еталон варто клонувати для звірки: `git clone --depth 1 https://github.com/v1frim/oxford_1000`.

---

## 2. Стек і жорсткі обмеження

| | |
|---|---|
| Технології | чистий HTML/CSS/JS, **без фреймворків, збірок і npm у рантаймі** |
| Логіка | **один** inline-скрипт в `index.html` |
| Дані-конфіг | окремі `.js`: `config.js`, `tasks.js` (щоб `index.html` не розпухав) |
| Стан користувача | тільки `localStorage`, без сервера |
| Хостинг | GitHub Pages, гілка `main`, папка `/(root)` |
| Гіт | коміти **напряму в `main`**, без dev-гілок і PR |
| Мова | інтерфейс, коментарі в коді й спілкування — українська |
| Платформа | **ПК** (рішення сесії 1). Мобільний — лише «не розсипається» |

---

## 3. Ухвалені рішення (сесія 1, відповіді користувача)

| # | Питання | Рішення | Наслідок |
|---|---|---|---|
| 1 | Спосіб запису задачі | **Гібрид** | Головна дія — поле «Що зробив?» + ряд кнопок складностей: один тап створює **і одразу закриває** задачу. Блок «У роботі (N)» існує, але **згорнутий за замовчуванням** |
| 2 | Темп | **2–5 задач/день**, до 8–10 у дні дрібних | План за замовчуванням 4/день, 80/місяць |
| 3 | Кількість рівнів | **100** (користувач сказав «100 або 1000») | 10 ліг × 10 рівнів. Рівень 100 ≈ 250k XP ≈ 6–7 років за поточним темпом |
| 4 | Пристрої | **тільки ПК** | Імпорт із повною заміною, без режиму злиття. PWA — в кінець |
| 5 | Межа доби | **календарна північ** | `dayCutHour: 0`, але параметр лишається в `workquest_plan_v1` |
| 6 | Де живуть рекорди | **окремий оверлей** по кнопці 🏆 | Знімає суперечність ТЗ («оверлей» проти «третя колонка»). Одна реалізація, не дві |
| 7 | Що в правій колонці | **Поточні задачі** | Замість рекордів. Блок «У роботі» переїхав із центральної картки в праву колонку — картка лишається про дію, колонка про стан |
| 8 | Назви рівнів | **ліга + римський номер** | «Новачок I» … «Новачок X», далі «Підмайстер I». Кожен зі 100 рівнів має ім'я, і не треба вигадувати 100 назв |

⚠️ Рішення 3: рівень **ніколи не зберігається** — обчислюється з `xp.total`.
Тому перекалібрувати шкалу (хоч на 1000 рівнів) можна будь-коли, замінивши
масив `LEVELS` у `config.js`. Міграція не потрібна. Але робити це краще
**до** накопичення великої історії, щоб не було психологічного шва.

---

## 4. Три несучі архітектурні рішення

### 4.1 Журнал задач — єдине джерело правди
`workquest_tasks_v1` — це все. Днів, XP, серій, рекордів, топів у сховищі
**немає**: вони обчислюються одним `S()`. Це прибирає цілий клас багів еталона
(розсинхрон кількох сховищ на одну подію, родина міграцій «перерахунку XP»,
дірки в `BACKUP_KEYS`, «XP = 0 назавжди після імпорту»).

### 4.2 Виконана задача — імутабельний факт
Вона несе заморожені `xpAwarded`, `doneDay` і знімок складності `difSnap`.
Тому вимога «зміна XP складності не переписує вже нараховані бали» — не правило,
якого треба дотримуватись, а **властивість за побудовою**: у коді просто немає
місця, де XP перераховується з реєстру.

### 4.3 Одна точка мутації `commit()` + реєстр рендерів `RENDER`
Ручного переліку «яку панель перемалювати» в обробниках не існує. Це прямий фікс
граблі еталона: панель тихо не оновлювалась, бо в одному з 13 обробників забули рядок.

---

## 5. Структура файлів

| Файл | Призначення |
|---|---|
| `index.html` | Уся розмітка, весь CSS, один головний inline-скрипт |
| `config.js` | Реєстри-конфіг: `LEVELS`, `LEAGUES`, `DEFAULT_DIFFICULTIES`, `METRICS`, `ACHIEVEMENTS`, `PERIODS`, `WQ_TEXT` |
| `tasks.js` | Сід першого запуску, шаблони-рутини, підказки тегів, тексти порожніх станів |
| `verify.sh` | Передкомітна перевірка. Ганяти перед **кожним** комітом голим рядком |
| `check-config.js` | Вартовий цілісності реєстрів і контракту метрик |
| `loadtest.js` | Виконання головного скрипта під мок-DOM у `node:vm` (без npm) |
| `test-load.js` | Сторінка в справжньому Chromium через CDP (без npm). Немає браузера → пропуск |
| `CLAUDE.md` | Правила сесії |
| `handoff.md` | Цей файл |
| `.nojekyll` | GitHub Pages не чіпає файли |

### Порядок блоків головного inline-скрипта (жорсткий — запобіжник від TDZ)

```
§0  WQ_KEYS + K + BACKUP_KEYS + lsGet/lsSet/loadJSON   ← УСІ const ключів ТУТ
§1  дати: todayKey, dayKeyOf, parseKey, dayNum, weekKey, monthKey, fmt*
§2  esc/escAttr, plural, fmtNum, hexToRgba, _maxOf
§3  сховище сутностей: loadTasks/saveTasks, loadDifs/saveDifs, loadPlan, loadAch
§4  runMigrations() + migrateKey() + sanitizeData() + seedIfEmpty()
§5  S() + bumpStats() + metricValue()                  ← ЄДИНИЙ ОБЧИСЛЮВАЧ
§6  реєстр складностей: getDif, activeDifs, difSnapFor
§7  дії: createTask, completeTask, uncompleteTask, editTask, deleteTask
§8  звук (_beep, sfx*) + черга мілстоунів + showMilestone
§9  перевірки: checkLevelUp, checkRecords, checkPlan, checkAchievements
§10 RENDER-реєстр + refresh() + commit()
§11 делеговані обробники + хоткеї + бекап/імпорт + сторожі + INIT
```

---

## 6. Модель даних

### 6.1 Задача

```json
{
  "id": "t_lq9x2k_a7f",
  "title": "Полагодити білд CI",
  "note": "падає на кроці lint",
  "difficultyId": "d_hard",
  "tags": ["інфра"],
  "createdAt": 1756670400000,
  "doneAt": 1756681200000,
  "doneDay": "2026-08-31",
  "xpAwarded": 60,
  "difSnap": { "name": "Складна", "color": "#ffa94d", "icon": "🟠" }
}
```

| Поле | Правило |
|---|---|
| `id` | `"t_" + Date.now().toString(36) + "_" + rand36(3)`. Видається **один раз**, ніколи не перевидається. Не індекс, не заголовок |
| `title` | 1..200, `trim()`. У DOM **тільки** через `esc()`/`escAttr()` |
| `note` | 0..2000, те саме екранування |
| `difficultyId` | Може вказувати на **архівовану** складність — це нормально. Резолв тільки через `getDif(id)` |
| `tags` | trim + dedup + lowercase, макс 8, кожен ≤30 |
| `createdAt` | ms, незмінне |
| `doneAt` | ms \| `null`. `null` = відкрита. Ключ сортування й тай-брейку рекордів |
| **`doneDay`** | `"YYYY-MM-DD"` \| `null`. **Заморожений** локальний ключ доби. Уся денна статистика групується по ньому |
| **`xpAwarded`** | number \| `null`. **Заморожений** знімок `difficulty.xp` на момент виконання |
| **`difSnap`** | `{name,color,icon}` \| `null`. Знімок вигляду складності — фолбек, **не первинне джерело** |

**Інваріанти** (перевіряє `sanitizeData()` при кожному завантаженні):
1. `doneAt === null` ⟺ `doneDay === null` ⟺ `xpAwarded === null` ⟺ `difSnap === null`
2. `doneAt !== null` → `xpAwarded` число ≥0, `doneDay` валідний `YYYY-MM-DD`
3. `doneAt >= createdAt - 1000` (допуск на перекіс годинника)

### 6.2 ⚠️ Правило незмінності `xpAwarded`

```js
// ⚠️⚠️ ЗМІНА xp У РЕЄСТРІ СКЛАДНОСТЕЙ ДІЄ ЛИШЕ НА МАЙБУТНІ ВИКОНАННЯ.
// Реєстр редагує КОРИСТУВАЧ у рантаймі. Ретро-перерахунок або дарував би XP
// за торішні задачі, або відбирав уже зароблене.
function completeTask(t) {
  if (t.doneAt) return;
  const d = getDif(t.difficultyId);
  t.doneAt    = Date.now();
  t.doneDay   = dayKeyOf(t.doneAt);            // ← межа доби вирішується ОДИН раз
  t.xpAwarded = d ? d.xp : 0;                  // ← ЗНІМОК
  t.difSnap   = { name: d.name, color: d.color, icon: d.icon };
  commit();
}
// Симетрія: знімаємо РІВНО стільки, скільки дали. Ніколи не поточний d.xp.
function uncompleteTask(t) {
  if (!t.doneAt) return;
  t.doneAt = t.doneDay = t.xpAwarded = t.difSnap = null;
  commit();
}
```

Механічні гарантії:
- `xp.total` рахується **виключно** як `Σ t.xpAwarded`. Ключа `workquest_xp_v1` не існує.
- Редагування **виконаної** задачі дозволене лише для `title`/`note`/`tags`.
  Складність — ні; тільки через «↩ повернути в роботу → змінити → виконати заново».
- У редакторі складностей під полем XP — постійний підпис:
  *«Нове значення діє лише на майбутні виконання; уже виконані задачі зберігають свій XP»*.

### 6.3 ⚠️ Видалена складність

**Видалення = архівація.** Три рівні захисту одночасно:

| Рівень | Що дає |
|---|---|
| `archived: true` (тумбстон) | Складність зникає з кнопок і форм, але жива в реєстрі. Осиротілих посилань не існує за побудовою: `getDif(id)` завжди знаходить запис. Історія не сіріє |
| `difSnap` у задачі | Другий фолбек, якщо реєстр зіпсовано вручну |
| Третій фолбек у `getDif()` | `{name:"Невідома", color:"#6b7a99", icon:"⬜", xp:0, missing:true}` — сірий плейсхолдер замість падіння |

⚠️ **Живий реєстр виграє завжди; `difSnap` — тільки для того, чого в реєстрі
більше немає.** Інакше після перейменування складності рядок дня покаже дві
різні назви для однієї складності.

Правила редактора:
- **Останню активну складність архівувати не можна.**
- Є **відкриті** задачі → діалог: «перенести в ⟨вибір⟩» / «лишити як є» / «скасувати».
- **Справжнє видалення** — лише коли по складності 0 задач узагалі.
- `order` — цілі числа, перевпорядкування **кнопками ↑/↓** (не drag&drop:
  працює з клавіатури, лягає в делегований обробник, немає стану перетягування).

### 6.4 Реєстр ключів localStorage

⚠️ **Новий ключ додається ТІЛЬКИ в `WQ_KEYS`.** `BACKUP_KEYS` генерується з нього,
тож баг «забув додати ключ у бекап» неможливий.

| Ключ | Клас | Що всередині |
|---|---|---|
| `workquest_meta_v1` | data | `{schema, createdAt, app}` — версія схеми їде разом із даними |
| `workquest_tasks_v1` | data | масив задач (≈250 Б/задача) |
| `workquest_difficulties_v1` | data | редагований реєстр складностей |
| `workquest_plan_v1` | data | `{perDay, perMonth, topMark, basis, dayCutHour}` |
| `workquest_ach_v1` | data | `{achId: найвищий оголошений рівень}` — тільки росте |
| `workquest_event_log_v1` | data | append-only страховка, кап 5000 |
| `workquest_trash_v1` | data | видалені виконані задачі; у метриках не рахуються |
| `workquest_records_best_v1` | derived | база порівняння «Новий рекорд!» |
| `workquest_levelup_at_v1` | derived | мітка часу для годинного підсвічування |
| `workquest_backup_date_v1` | derived | дата останньої копії (галочка на кнопці) |
| `workquest_prog_open_v1` | ui | стан панелей |
| `workquest_rec_period_v1` | ui | обраний період рекордів |
| `workquest_last_dif_v1` | ui | остання складність (для Enter) |
| `workquest_sfx_v1` | ui | вимикач звуку |
| `workquest_seed_v1` | flag | сід застосовано |

⚠️ **`BACKUP_KEYS` = УСІ ключі** (пряма вимога ТЗ). Клас впливає лише на
пост-імпортні виправлення, не на склад бекапу. Так після відновлення не губляться
ні налаштування панелей, ні вимикач звуку, ні здобуті ліги досягнень.

**Чого свідомо НЕМАЄ:** `workquest_xp_v1`, `workquest_days_v1`,
`workquest_streak_v1`, `workquest_top_v1`. Усе це похідне від `S()`.
Одна подія = один запис в один масив.

### 6.5 Час і межа доби

```js
// ⚠️ ЄДИНЕ джерело «сьогодні» на весь проєкт. ЛОКАЛЬНИЙ час.
// toISOString().slice(0,10) дав би UTC: для Києва з 00:00 до 03:00 день і МІСЯЦЬ
// вважались би попередніми. Заборонено; ловить греп у check-config.js.
function fmtKey(d){ return d.getFullYear()+"-"+String(d.getMonth()+1).padStart(2,"0")+"-"+String(d.getDate()).padStart(2,"0"); }
function todayKey(){ return fmtKey(dayShifted(new Date())); }
function dayKeyOf(ts){ return fmtKey(dayShifted(new Date(ts))); }
const parseKey = k => new Date(k + "T00:00:00");   // ⚠️ голий ISO парситься як UTC-північ
const dayNum   = k => Date.UTC(+k.slice(0,4), +k.slice(5,7)-1, +k.slice(8,10)) / 86400000;
```

Арифметика днів — тільки через цілочисельний `dayNum()`. Сусідство днів у серіях —
`Math.round(diff) === 1`, ніколи `diff === 1` (доба буває 23 або 25 годин через DST).

**Чому `doneDay` заморожений:** обчислення на льоту «попливло б» ретроспективно
у трьох сценаріях — переїзд у інший пояс, зміна `dayCutHour`, DST. Закриті серії
днів порвались би заднім числом. Заморозка коштує 12 байт на задачу.

### 6.6 Версіонування і міграції

1. Суфікс `_v1` у **кожного** ключа з першого дня.
2. Основний механізм — **поліморфний читач**, а не бамп версії ключа:
   ```js
   const tXp   = t => (typeof t.xpAwarded === "number") ? t.xpAwarded : 0;
   const tTags = t => Array.isArray(t.tags) ? t.tags : [];
   const tDone = t => (typeof t.doneAt === "number" && t.doneAt > 0);
   ```
   Старі бекапи читаються без міграції.
3. Лічильник схеми в `workquest_meta_v1` + `MIGRATIONS = {1: fn, 2: fn, …}`.
   Номер схеми пишеться **останнім рядком усередині `try`** — інакше при падінні
   дані лишаються в напівстані назавжди.
4. `migrateKey(oldKey, newKey, transform)` — хелпер на випадок, якщо ключ усе ж
   доведеться перейменувати. Копіює, перевіряє, і **лише потім** видаляє старий.
5. `sanitizeData()` — ідемпотентний санітар без прапора, ганяється щоразу.
6. Міграція впала → **банер** «Оновлення даних не завершилось. Зробіть копію
   перед подальшою роботою» + жодних деструктивних дій. **Ніколи не мовчазна втрата.**

---

## 7. Контракт метрик

**Залізне правило:** жоден рендер, жодне досягнення, жоден рекорд **не читає
localStorage сам**. Усе через `S()`. Кеш скидає `bumpStats()` — перший рядок
`commit()`, тобто в одному місці на весь проєкт.

```js
// metricValue("tasks.doneByDiff.d_hard") → число. Крапковий шлях у S().
// ⚠️ Повертає 0 (не undefined) для відсутнього листа: «ще нема даних» не має
// ламати ні смужку (width:NaN%), ні порівняння рекорду.
```

Один прохід O(n) будує `days`/`weeks`/`months`/`byDiff`/`byTag`/`byWeekday`,
далі три дешеві похідні проходи. 5000 задач ≈ 2–3 мс.

### Каталог метрик (ключове)

| Ключ | Як рахується |
|---|---|
| `tasks.done` / `.open` / `.total` | лічильники по журналу |
| `tasks.doneByDiff.<difId>` | включно з архівованими складностями |
| `tasks.doneByTag.<tag>` | по кожному тегу |
| `xp.total` | `Σ tXp(t)` — **єдине джерело XP у проєкті** |
| `xp.byDiff.<difId>` | XP у розрізі складності |
| `days.active` | днів із ≥1 виконаною |
| `days.calendar` | `dayNum(today) − dayNum(days.first) + 1` |
| `today.done` / `today.xp` / `yesterday.done` | по `days[…]` |
| `week.*` / `month.*` | поточний ISO-тиждень (пн-старт) / календарний місяць |
| `streak.current` | серія, що закінчується **сьогодні**; інакше 0 |
| `streak.best` / `.bestEnd` / `.atRisk` | рекордна серія, дата кінця, «під загрозою» |
| `best.dayDone` / `best.dayXp` (+ `…At`) | рекорди дня + дати |
| `best.dayXpFair` | рекорд XP за день **без епіків** — щоб одна велика задача не зробила планку недосяжною назавжди |
| `best.dayDoneByDiff.<difId>` | окрема планка на кожну складність |
| `best.weekDone` | найкраще ковзне вікно 7 днів |
| `best.monthDone` / `best.monthXp` | найкращий календарний місяць |
| `best.taskXp` | найдорожча закрита задача |
| `avg.donePerCalendarDay` | `tasks.done / days.calendar` — **дільник КАЛЕНДАРНИЙ** (вимога ТЗ), дні без активності входять |
| `avg.donePerActiveDay` | довідково, у тултипі |
| `avg.donePerMonth` / `avg.xpPerMonth` | по календарних місяцях від першого дня |
| `weekday.avgDone[0..6]` | Σ по дню тижня / кількість **календарних** таких днів |
| `weekday.best.*` | argmax по середньому. ⚠️ **За середнім, не за сумою**: суму виграє день, якого просто більше минуло |
| `level.n` / `.name` / `.icon` / `.league` / `.pct` / `.xpInLevel` / `.xpToNext` | з `getLevelInfo(xp.total)`; на максимумі `pct = 100` **явно** |
| `plan.dayPct` / `.monthPct` / `.dayDone` | виконання планів |
| `hours.early` / `.late` | задач до 10:00 / після 20:00 |

### Як розширювати

- **Нова метрика — 3 кроки:** порахувати поле в `S()` → оголосити в `METRICS`
  → (за потреби) додати в `RECORD_KEYS`.
- **Нове досягнення — 1 крок:** дописати об'єкт у `ACHIEVEMENTS` (`config.js`):
  ```js
  { id:"a_done", icon:"✅", title:"Робоча машина", desc:"Виконуй задачі",
    metric:"tasks.done", tiers:[25,100,300,750,1500] }
  ```
  `metric` — **рядок-ключ із `METRICS`**, не замикання. Користувач не пише код.
  `check-config.js` валить `verify.sh` на одруківці в `metric`.

`METRICS[key].kind` автоматично визначає поведінку картки:
- `cumulative` → рівень рахується наживо, смужка = прогрес **у межах поточної ліги**;
- `periodic` → рівень із збереженого `claimed` (скидання серії/місяця лігу
  **не знижує**), смужка **від нуля** до наступного порогу.

### Точка мутації

```js
const RENDER = { card, xp, tasks, days, totals, thermo, records, ach, difs };
function refresh(...z){ for (const n of (z.length ? z : Object.keys(RENDER))) RENDER[n](); }

// ⚠️ ЄДИНА ТОЧКА МУТАЦІЇ. Будь-яка зміна даних проходить ТІЛЬКИ через неї.
function commit(){
  saveTasks(); bumpStats(); refresh();
  checkLevelUp(); checkRecords(); checkPlan(); checkAchievements(true);
}
```
Точковий `refresh("records")` — лише для змін **без даних** (активний таб,
відкриття панелі, обраний чипс).

---

## 8. Рівні

- **100 рівнів, 10 ліг по 10 рівнів.** Масив порогів `LEVELS` у `config.js`,
  згенерований формулою і зафіксований (формула — у коментарі поруч).
- Рівень **ніколи не зберігається** — обчислюється з `xp.total`. Тому зміна
  шкали не потребує міграції.
- Прогрес-бар поточного рівня з підписом «X / Y XP», назва рівня, назва ліги.
- Свято при level-up: оверлей + звук + підсвічування XP-блоку **на годину**
  (`workquest_levelup_at_v1`).

---

## 9. Розкладка (за еталоном)

Зліва направо: **термометр планів** (вузька вертикальна колонка) · картка
**«Прогрес по днях»** · **центральна картка** · картка **«Поточні задачі»** ·
права панель **«Загальна статистика»** (fixed) · внизу зліва — кнопки-дії
(«Прогрес по днях», «🏆 Рекорди», «💾 Копія», «📂 Відновити», «⚙️»).

**Оверлеї** (спільний каркас `.sheet`: шапка з ✕, тіло зі скролом; закриття по
✕, кліку на затемнення і Esc):
- **🪜 Сходи рівнів** — клік по XP-блоку. Усі 100 рівнів згори вниз, згруповані
  по лігах; колір ліги фарбує бейдж, ліву межу й сяйво рядка. Поточний рівень
  підсвічений («ти тут · N XP»), список автоскролиться до нього. Майбутні
  рівні притлумлені (`opacity .42`).
- **🏆 Рекорди** — кнопка внизу зліва. Чипси періодів (7/30 днів/місяць),
  обраний зберігається в `workquest_rec_period_v1`.

⚠️ Кольори ліг у рядках рахує `hexToRgba()` з реєстру `LEAGUES`, **не**
`color-mix()`: той вимагає Chrome 111+, а тінт потрібен і як фон, і як межа,
і як сяйво — дешевше порахувати три rgba один раз при рендері.

Тема темна «космічна», акцент золотий/бурштиновий, заокруглені картки.

**Фон за порою дня + сезонами:** 4 фази (ранок/день/вечір/ніч), межі фаз залежать
від пори року, атрибут `data-tod` на `<html>` ставить **ранній** inline-скрипт
до першого кадру. Параметр `?tod=morning|day|evening|night` для примусової перевірки.
⚠️ Переходу-анімації між фазами немає **свідомо**: багатошарові фони
інтерполюються дискретно і смикаються.

⚠️ Плаваючі контроли — **солід + тінт**, не прозорість:
`linear-gradient(0deg,C,C), rgba(10,14,30,0.92)`. Інакше на світлих фазах доби
кнопки зникають. `backdrop-filter` не використовується взагалі.
Для складностей із користувацьким кольором потрібен `hexToRgba(hex, alpha)`:
`--dif` (сам hex), `--dif-t` (0.13), `--dif-b` (0.34).

---

## 10. Етапи

| # | Етап | Стан |
|---|---|---|
| 0 | Каркас і процес: `CLAUDE.md`, `handoff.md`, `verify.sh`, `loadtest.js`, `check-config.js`, `test-load.js`, скелет `index.html` із фоном і трьома колонками, `config.js`, `tasks.js` | ✅ готово |
| 1 | **Ядро ритуалу**: ключі, дати, міграції, модель Task, повний `S()`, центральна картка, XP-блок, рівні | 🔄 наступний |
| 2 | **Збереження**: бекап, відновлення, квота, приватний режим, журнал подій | ⬜ |
| 3 | **Редактор складностей**: кольоровий пікер, XP, порядок, архівація | ⬜ |
| 4 | **Історія**: «Прогрес по днях» (7 днів → тижні → місяці → роки), загальна статистика | ⬜ |
| 5 | **Святкування і рекорди**: звук, оверлей віх, вкладки рекордів, побиття рекордів | ⬜ |
| 6 | **Досягнення**: екран, багаторівневі пороги, тихий сід | ⬜ |
| 7 | **Плани і термометр** | ⬜ |
| 8 | **Аудит і поліровка**: скріншоти 4 фаз, порожні стани, хоткеї, PWA | ⬜ |

Кожен етап закінчується зеленим `./verify.sh` + комітом у `main` + **робочим сайтом**.
Ніколи не буває «наполовину переїхали».

---

## 11. Що свідомо відкладено

| Що | Чому |
|---|---|
| Дедлайни, нагадування, «прострочено» | ТЗ прямо каже «не менеджер задач». Червоні мітки вбивають ритуал |
| Повторювані задачі | Ламає модель одноразової події. Частково закрито шаблонами-рутинами в `tasks.js` |
| Підзадачі, проєкти, ієрархія | Теги покривають 90% потреби |
| Синхронізація між пристроями | Користувач працює тільки з ПК (сесія 1). Merge-режим реалізовний (задачі імутабельні), але не потрібен |
| Таймер / помодоро | Окрема підсистема часу |
| Графіки, спарклайни | «Прогрес по днях» + термометр дають щільнішу картину без бібліотек |
| Drag&drop сортування | Кнопки ↑/↓ роблять те саме й працюють із клавіатури |
| **Перерахунок XP «під нову шкалу»** | **Ніколи.** Прямий шлях до втрати зароблених балів |
| Мультиколірні теми | Тема одна, і вона — половина відчуття продукту |

---

## 12. ⚠️ Граблі — перевірено на еталоні, не наступати вдруге

### Дані
- **Дірка в `BACKUP_KEYS`** — в еталоні їх було чотири, і одна вбивала XP назавжди.
  → Список **генерується** з `WQ_KEYS`, `check-config.js` звіряє.
- **Прапор міграції поза `try`** → дані навіки в напівстані.
  → Номер схеми пишеться останнім рядком **усередині** `try`.
- **Порожній `catch(e){}`** маскує TDZ як «дані зникли».
  → Усі `const *_KEY` у §0; `catch` завжди логує.
- **Дві вкладки браузера** — вкладка B перезаписує масив своєю застарілою копією,
  задача з вкладки A зникає тихо.
  → Слухач `storage` + сентинел ревізії перед записом.
- **Кеш `S()` і перехід через опівніч** — вкладка, залишена з вечора, показує
  вчорашні цифри як сьогоднішні й ламає серію.
  → Сторож `setInterval` на 60 с: `todayKey()` змінився → `bumpStats(); refresh()`.

### Логіка
- **Асиметрія undo** (дали 10, зняли 150) = фарм XP.
  → `uncompleteTask` знімає **рівно `t.xpAwarded`**.
- **Вітання спамлять** на завантаженні й після імпорту.
  → Тихий сід рекордів, `checkAchievements(false)` в init, `prev > 0`,
  `_prevLevel !== null`, high-water не опускається.
- **Рекорд «XP за день» недосяжний** після однієї великої задачі.
  → Паралельна метрика `best.dayXpFair` + планка на кожну складність.
- **Ділення на нуль**: `days.calendar === 0`, `weekday` без даних, `pct` на
  максимальному рівні. → Явні перевірки; `metricValue` повертає 0, не `undefined`.

### Верстка
- **`.hidden` не працює**, якщо `display` заданий ID-селектором.
  → Не задавати `display` в ID-селекторах; інакше парне `#id.hidden{display:none}`.
- **`<input>` усередині перемальовуваного контейнера** збиває фокус на кожній літері.
  → Рядок, який редагується, не перемальовується; шапка й тіло рендеряться окремо.
- **Коментар `//` у кінці рядка всередині `+`-конкатенації** з'їдає `+`, і половина
  панелі тихо зникає. Ні синтаксис, ні loadtest цього не ловлять.
  → **Усі HTML-збірки через масив + `.join("")`.**
- **XSS через назву задачі** — дані вводить користувач, рендер через `innerHTML`.
  → `esc()` у тілі, `escAttr()` в атрибутах, греп-вартовий у `check-config.js`.

### Процес
- **Пайп у `verify.sh` маскує exit-код** — в еталоні двічі пройшов коміт із дефектом.
  → Вивід у файл, `|| exit 1`, `tail` окремим рядком. Кликати голим рядком.
- **Абсолютний шлях у `verify.sh`** (жива граблина еталона — посилався на шлях
  мертвої сесії). → `cd "$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"`.
- **`new Function` не ловить TDZ/ReferenceError** → `loadtest.js`, який реально
  виконує скрипт і викликає `S()` + `refresh()`.
- **`loadtest.js` не бачить помилок консолі в браузері** → `test-load.js` на CDP.

---
---

# ЧАСТИНА II · ЖУРНАЛ СЕСІЙ

## Сесія 1 — проєктування і каркас

**Що зроблено**

| # | Що | Привід користувача |
|---|---|---|
| 1 | Клоновано еталон `v1frim/Oxford_1000`, розібрано 10 підсистем до рядків коду | «хочу такий самий підхід, вигляд і рівень продуманості» |
| 2 | Спроєктовано архітектуру з трьох незалежних кутів (модель даних / щоденний ритуал / розширюваність), зведено в один план, окремо прогнано критику повноти | «спершу покажи мені план» |
| 3 | Створено `CLAUDE.md` і `handoff.md` | прямий запит |
| 4 | Створено `verify.sh` + `check-config.js` + `loadtest.js` + `test-load.js` | прямий запит |

**Рішення користувача** — див. розділ 3 довідника.

**Що з'ясувалось про еталон (важливе для перенесення)**
- CSS-змінних (`:root{--x}`) в еталоні **немає взагалі** — уся палітра хардкод-літералами,
  повтореними десятки разів. У Work_Quest робимо токени з самого початку.
- **Мобільної адаптації в еталоні немає** — усього 5 `@media`, з них один
  `min-width:1500px`. Тож переймати нема чого; для Work_Quest це й не пріоритет (тільки ПК).
- Тижні в «Прогресі по днях» еталон **прибрав** у сесії 27, лишивши мертвий код
  `_progMondayKey`/`_progFmtRange`. ТЗ Work_Quest тижні **вимагає** — робимо
  чотири рівні згортання (7 днів → тижні → місяці → роки) і дивимось на реальних даних.
- Фон за порою дня: `data-tod` на `<html>` з раннього inline-скрипта, без
  localStorage, без астрономії — таблиця порогів за сезонами. Переносимо як є.

### Етап 0 — зроблено

`index.html` (скелет), `config.js`, `tasks.js`, `.nojekyll`, `.gitignore`.

Що вже стоїть на місці:
- **Токени в `:root`** — уся палітра в одному місці (в еталоні їх не було зовсім).
- **Космічний фон + 4 фази доби** з сезонними межами, перенесено дослівно.
  Таблиця меж звірена з еталоном програмно, збіг повний:
  зима ранок 07–08 / день 09–14 / вечір 15–16; весна й осінь 06–07 / 08–17 / 18–20;
  літо 05–06 / 07–19 / 20–22.
- **Ранній inline-скрипт**: `data-tod` до першого кадру, `?tod=` форс,
  fallback `.ready` на `load`, проба `LS_OK` для приватного режиму.
- **Три колонки + три fixed-острови**, компенсація `balance-center` перевірена:
  центр картки збігається з центром екрана на 1920.
- **Реєстр `WQ_KEYS`** (16 ключів) → `BACKUP_KEYS` генерується.
- **`RENDER`-реєстр і `refresh()`** уже на місці — етап 1 просто підмінить
  заглушки на справжні дані.
- `config.js`: 100 рівнів, 10 ліг, 4 складності, 37 метрик, 10 досягнень.

**Граблі етапу 0**

1. **Стенд виконував лише найдовший inline-скрипт** (як в еталоні) — і падав
   із `ReferenceError: LS_OK is not defined`, бо `LS_OK` оголошений у ранньому
   скрипті. Виправлено: `loadtest.js` тепер виконує **всі** inline-скрипти
   в порядку документа, як браузер.
2. **Хибний діагноз «біла смуга внизу»**. У headless-знімку сторінка не
   заповнювала кадр, і це виглядало як недопрацьований фон. Заміряно:
   `innerHeight = 993` при `--window-size=1920,1080` — тобто **артефакт знімка**
   (кадр робиться за розміром вікна, а не viewport), сторінка заповнює viewport
   рівно (`html = body = scroll = 993`). Фон лишається на `body`, як в еталоні.
   ⚠️ Не «виправляти» це знову: знімай через CDP із
   `Emulation.setDeviceMetricsOverride`, тоді кадр = viewport.
3. **Анімація появи в кадрі.** `.layout` має `transition: opacity .25s`, і знімок
   через `--screenshot --virtual-time-budget` ловив її на півдорозі — картки
   виглядали напівпрозорими. Знімати треба з реальною паузою ~2.5 с.

**Наступний крок:** етап 1 — ядро ритуалу.
